=== EVIDENCE 02: .gitignore blocks the bootstrap run's own deliverable from being committed ===

--- committed .gitignore (HEAD) ---
.omc/

--- working tree .gitignore (uncommitted, part of this run's branch) ---
.omc/
specs/
docs/
--- git status --short (note specs/ and docs/ do NOT appear as ?? even though they contain new files) ---
 M .gitignore
?? CHANGELOG.md

--- git status --short --ignored (proves they are ignored, not merely 'clean') ---
 M .gitignore
?? CHANGELOG.md
!! .omc/
!! docs/
!! specs/

--- attempt: git add -n specs/ docs/ (dry run) ---
The following paths are ignored by one of your .gitignore files:
docs
specs
hint: Use -f if you really want to add them.
hint: Disable this message with "git config set advice.addIgnoredFile false"
exit code: 1

--- git check-ignore -v on the three key deliverable files ---
.gitignore:2:specs/	specs/LOOP_STATE.md
.gitignore:3:docs/	docs/README.md
.gitignore:3:docs/	docs/evidence/2026-08-04-bootstrap/01-token-baseline.txt

--- docs/flows/loop-run.md line 52 claims persistence via commit ---
20:| 0 | Orientation: check the branch, finish any uncommitted prior run, read learnings → state → changelog → readme, plus Claude Code memory | a 4-line summary in the report |
52:work does: everything the run learned is in `specs/` and `docs/`, committed. A fresh

--- README.md claims the same ---
88:so it stops when the session ends — the work does not.
