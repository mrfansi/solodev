===== SEAM 2 (corrected) =====
NOTE: the first attempt (01/02) used coloured, unsorted `ls` output through
comm and reported every shipped skill as 'NAMED BUT ABSENT'. That result was
wrong and is discarded. Corrected below with LC_ALL=C sort on clean input.

-- named in docs: 15   shipped: 15
-- named but not shipped:
-- shipped but never named in docs:
   (both lists empty = every promise has a keeper and nothing ships undocumented)

-- protocol §6: docs/ must hold 'one file per major user flow'
   flow docs present : 1  ->  loop-run.md 
   user-invocable skills: 11
   skills with no flow doc mentioning them:
     pr-review
