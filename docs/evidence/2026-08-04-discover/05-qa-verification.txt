QA VERIFICATION of /solodev:discover, run #2
Date: 2026-08-04. QA pass independent of the run that shipped the feature.
Method: re-executed the section-2 seam commands from skills/discover/SKILL.md
against the live repo (branch loop/run-2-backlog-discovery, worktree as committed
plus uncommitted diffstat matching `git status --short` at QA start), and checked
every claim in 04-discover-report.txt against the repo by hand.

===== GATE =====
$ python3 scripts/validate.py
skills: 11  agents: 5 (4 without edit tools)
OK
Exit: 0

===== TIGHTEST CONDITION: is C-6 real, non-duplicate, and is the drop legitimate =====

C-6 cite target: specs/LOOP.md §6 "DOCUMENTATION STANDARD" -> docs/ subsection ->
"one file per major user flow". Confirmed present verbatim at specs/LOOP.md:364.

Flow docs, counted directly:
$ ls docs/flows/
loop-run.md
-> 1 file. Matches "flow docs present : 1".

Skills, counted directly:
$ find skills -mindepth 1 -maxdepth 1 -type d | wc -l
11
-> matches "user-invocable skills: 11".

pr-review naming check (the specific skill named as uncovered):
$ grep -noE "pr-review[a-z]*" docs/flows/loop-run.md
28:pr-reviewer
34:pr-reviewer
-> only the AGENT name "pr-reviewer" appears; the SKILL "pr-review" (exact token)
   never appears. C-6's claim holds under exact-token scrutiny, not just substring.

Dedup check — does "flow doc" / "docs/flows" / "one file per" appear anywhere else
in specs/LOOP_STATE.md before this run's own C-6 row?
$ grep -n -i "flow doc\|docs/flows\|major user flow\|one file per" specs/LOOP_STATE.md
79:C-6 row itself
142:call-site inventory row (not a backlog item)
-> No prior open row, no struck-through row, no "Considered and rejected" entry
   duplicates C-6. Genuinely new.

Dropped seam-4 item vs C-4(b) — is it really the same finding?
C-4(b) (backlog, filed run #1 Phase 8 by pr-reviewer):
  "docs/architecture.md to the file list the agent-count regex already scans —
  it currently checks only README.md and skills/loop/SKILL.md"
Dropped seam-4 candidate (04-discover-report.txt):
  "docs/architecture.md carries the claim, but scripts/validate.py:137
  cross-checks that claim only in README.md and skills/loop/SKILL.md"
$ grep -n "README.md\|SKILL.md" scripts/validate.py | tail -3
137:for path in (ROOT / "README.md", ROOT / "skills/loop/SKILL.md"):
-> Confirmed identical finding, confirmed line 137 is exactly the two-file list
   cited. The drop is a genuine duplicate, not a lost finding.

BACKLOG arithmetic:
Open (non-struck) ids before C-6: B-2 C-1 C-3 C-4 C-5 E-2 E-3 R-1 R-2 R-3 = 10
Open ids after C-6: + C-6 = 11
-> "11 open items, was 10" reproduces exactly.
Score ranking check: B-2=20.0, C-4=12.0, C-5=12.0, R-2=10.0, R-3=10.0, C-3=9.0,
R-1=8.3, E-3=6.0, E-2=5.0, C-6=4.0, C-1=4.0
-> "Top of queue unchanged: B-2, then C-4 and C-5" reproduces.

===== SEAM 2 REPRODUCTION (15 named / 15 shipped) =====
NOTE: `ls` in this environment is aliased to `eza --color=always`, which is
exactly the bug the run's own transcript 02 disclosed and 03 corrected. This QA
pass hit the SAME bug on first attempt using `ls`, confirming the run's account
of transcript 02's defect is accurate and reproducible, not invented after the
fact:

$ type ls
ls is an alias for eza --color=always --git --icons=always

Corrected reproduction (find instead of ls, matching 03's fix):
$ NAMED=$(grep -rohE 'solodev:[a-z][a-z-]*' README.md docs/ CHANGELOG.md \
    skills/*/SKILL.md agents/*.md specs/*.md | sed 's/solodev://' | LC_ALL=C sort -u)
$ SHIPPED=$( (find skills -mindepth1 -maxdepth1 -type d -exec basename {} \; ; \
    find agents -mindepth1 -maxdepth1 -name '*.md' -exec basename {} .md \;) | LC_ALL=C sort -u)
named count: 15
shipped count: 15
named but not shipped: (empty)
shipped but not named: (empty)
-> matches 03-seam-2-corrected.txt and 04's "15 named, 15 shipped, both comm
   directions empty" exactly. 03 corrects 02, and 04 rests on 03's numbers, not
   02's — confirmed by cross-reading: 04 never repeats 02's "NAMED BUT ABSENT"
   list.

===== SEAM 1 REPRODUCTION — DISCREPANCY FOUND =====
Command exactly as specified in skills/discover/SKILL.md section 2, row 1:
$ grep -rnE '(TODO|FIXME|HACK|XXX)' --include='*' .
CHANGELOG.md:14:  needed but nobody filed. It works six seams — abandoned `TODO`/`FIXME` markers,
specs/LOOP.md:25:2. **No new placeholders, stubs, or `TODO`s.** Those are blockers, not progress.
specs/LOOP.md:387:- [ ] No new stubs, placeholders, or `TODO`s
docs/evidence/2026-08-04-discover/01-seam-sweep.txt:5:CHANGELOG.md:14: ... (self-match, transcript file itself)
skills/discover/SKILL.md:49:| 1 | Abandoned markers | `grep -rnE ...` | ... (self-match, the seam table)
skills/loop/references/PROTOCOL.md:25:2. **No new placeholders, stubs, or `TODO`s.**
skills/loop/references/PROTOCOL.md:387:- [ ] No new stubs, placeholders, or `TODO`s

specs/LOOP.md and skills/loop/references/PROTOCOL.md are BOTH pre-existing,
committed in run #1 (commit 5ef3122) and af752a3 respectively — confirmed via
`git log`, and confirmed neither file appears in run #2's `git status --short`
modified list. They existed, unmodified, at the time run #2's seam-1 sweep ran.

01-seam-sweep.txt and 04-discover-report.txt both state: "Sole grep hit is
CHANGELOG.md:14" / "seam 1 (abandoned markers): clean. Sole grep hit is
CHANGELOG.md:14, this run's own prose describing the seam." This is false as a
description of the raw command's output — the same command against the same
pre-existing files produces at least 4 more hits (specs/LOOP.md:25/387,
skills/loop/references/PROTOCOL.md:25/387). These are true false positives
(policy prose about TODOs, not actual abandoned markers), so excluding them
from FILED was the right call — but the transcript never discloses that they
were found and excluded, unlike seam 6, which explicitly names and explains
its one false positive ("git log --grep=revert -i matched 5ef3122..."). Seam 1
gets no equivalent disclosure; instead it asserts a raw-hit-count that is
provably wrong for the repo state at sweep time.

===== SEAM 3 REPRODUCTION =====
$ grep -rn 'backlog\|deferred\|not fixed\|out of scope' docs/evidence/2026-08-04-bootstrap/
docs/evidence/2026-08-04-bootstrap/06-token-baseline-corrected.txt:12: ...backlog...
docs/evidence/2026-08-04-bootstrap/06-token-baseline-corrected.txt:24: ...the backlog...
Neither matched line names an id. R-3 appears on line 29 of the same file
("=> filed as R-3 ..."), reached only by reading context around the grep hits
per the seam's own instruction ("then check each against the backlog table").
This is legitimate methodology, not a fabrication — recorded here because it
looked suspicious on first pass and needed the context check to clear it.
Cross-check: every id appearing anywhere in docs/evidence/2026-08-04-bootstrap/
(B-1, B-2, C-3, R-3) is present in the current backlog table. Seam 3's "clean"
verdict holds.

===== SEAM 4 / VALIDATOR BLIND SPOT =====
$ grep -n "README.md\|SKILL.md" scripts/validate.py | tail -3
137:for path in (ROOT / "README.md", ROOT / "skills/loop/SKILL.md"):
Confirms the cited line and the two-file list exactly.

===== SEAM 5 =====
$ grep -c "^Run #" specs/LOOP_STATE.md   (within ## Run Log section)
1
Matches "1 run recorded; a repeat needs 2+" / UNAVAILABLE verdict.

===== FRONTMATTER KEY CHECK =====
skills/discover/SKILL.md frontmatter uses only `name` and `description`.
Both appear in specs/REFERENCE.md's researched key table (`name`: No,
`description`: Recommended). No invented key.

===== STALE-CLAIM / DOD CHECK =====
specs/LOOP_STATE.md "Definition of Done for this iteration": all 10 checkboxes
are unticked (`- [ ]`), including:
  "The resident description cost is re-measured after adding the 11th skill,
  and the delta is stated"
This was NOT done. specs/REFERENCE.md:68 still reads "Ten skills cost ~954
tokens of permanently resident context in this plugin (measured run #2)" —
after this same run shipped the 11th skill (discover). Measured discover's own
description field: 354 chars / ~88 tokens, on top of the ~954 baseline — a
~9% increase never folded into the headline number, and the DoD item that
required doing so was left unchecked rather than struck or deferred with a
note.

Everything else in the DoD list was independently verified true (skill exists
with valid frontmatter; row format matches task; every filed item cites
evidence; dedup ran against open + struck + rejected; cap of 5 respected
(1 filed); validator green at 11 skills; skill executed against the repo in
Phase 4; architecture.md's stale claim rewritten; README/CHANGELOG/docs
updated) — only the last checkbox's underlying work is actually missing, but
none of the ten are ticked, so the checklist itself is unreliable as a
completion signal.

===== ARCHITECTURE / README CONSISTENCY =====
$ grep -rn "nothing decides\|decides what to build\|what to build\|deliberately missing" --include='*.md' . | grep -v docs/evidence
No file outside skills/discover/SKILL.md, CHANGELOG.md (both describing the
NEW capability), and specs/LOOP_STATE.md (planning notes) still asserts the
old gap. docs/architecture.md's replacement section ("Two ways work enters the
backlog") reads consistently with skills/discover/SKILL.md and skills/task/SKILL.md
— no restated scoring formula, id sequence, or severity table found in discover.
docs/README.md's doc index links (architecture.md, flows/loop-run.md,
evidence/) all resolve.
