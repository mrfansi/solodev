Check: any claim in docs/architecture.md's rewritten boundary section that the
repo contradicts.

docs/architecture.md lines 134-135 (new, this run):
"What is still missing: nothing decides *when* to release. `ship` cuts a
version when asked; no cadence or rule proposes one."

specs/LOOP.md §3 Phase 8, step 3 (pre-existing, unchanged by this run):
"3. **Version** — user-visible change -> bump SemVer and move `[Unreleased]`
into a version section."

This IS a rule that proposes when to release: on every run that ships a
user-visible change, i.e. on essentially every loop iteration. It is not new
— it predates this run and is the exact defect this run's own
specs/LOOP_STATE.md documents at length as motivation for shipping `ship`:

specs/LOOP_STATE.md lines 158-169 (Current task section, written this run):
"Protocol §3 Phase 8 step 3 mandates: 'user-visible change -> bump SemVer and
move [Unreleased] into a version section.' Three runs have shipped
user-visible changes. There are zero version sections, zero tags, and the
version is still 0.1.0. The protocol has been mandating a step that no skill
supports and no run has performed."

So within the SAME set of files this run touches: LOOP_STATE.md correctly
describes the protocol as ALREADY mandating a release-timing rule that simply
had no implementing skill (now fixed by `ship`); architecture.md's new
sentence instead claims no such rule exists at all. The two documents
disagree about the same fact, and LOOP.md's actual text sides with
LOOP_STATE.md, not architecture.md.

Severity: S3. A documentation-accuracy defect — misleads a reader of
architecture.md into thinking release timing is fully unowned, when the
protocol already owns it (just previously unenforceable). Does not affect any
executable behavior.
