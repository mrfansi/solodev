===== QA FIXES VERIFIED — run #4 =====

S2 — step 3's version search, old vs new
  old (greps the WORD 'version'):
    .claude-plugin/marketplace.json:13:      "version": "0.1.0",
    .claude-plugin/plugin.json:4:  "version": "0.1.0",
  new (greps the CURRENT version STRING):
    docs/architecture.md:137:three runs shipped user-visible changes while the version sat at `0.1.0`. `ship` gives
    .claude-plugin/plugin.json:4:  "version": "0.1.0",
    .claude-plugin/marketplace.json:13:      "version": "0.1.0",
    specs/LOOP_STATE.md:30:Plugin `solodev` v0.1.0. **Eleven skills**, five agents, one structure validator.
    specs/LOOP_STATE.md:160:version sections, zero tags, and the version is still 0.1.0.** The protocol has been
    specs/LOOP_STATE.md:167:latest version'."* Because `version` is set and pinned at 0.1.0, **everything runs #1–#3
    specs/LOOP_STATE.md:191:| `.claude-plugin/plugin.json` | version 0.1.0 → the bump this run cuts |
    skills/ship/SKILL.md:55:at `0.1.0`, gives **`0.2.0`** — MINOR, driven by `Added`, unshifted.
    skills/ship/SKILL.md:79:"version" finds the manifests and stops — it missed a `v0.1.0` sitting in a state file
  -> the new form finds specs/LOOP_STATE.md, which the old one structurally could not.

S1 — bump derivation is now single-valued
  [Unreleased] subsections present:
    ### Added
    ### Changed
    ### Fixed
    ### Internal
    -> Added present, Removed absent -> MINOR.
    -> below 1.0.0, only the MAJOR row shifts, so MINOR is unshifted.
    -> 0.1.0 -> 0.2.0. One answer, no reading required.

S3 — link refs: is this the oldest release?
    git tag --list 'v*' -> 0
    0 tags = this IS the oldest release, so /releases/tag/ is correct HERE.
    The skill now says so explicitly and branches for later releases.

===== GATE =====
skills: 12  agents: 5 (4 without edit tools)
OK
  exit=0
