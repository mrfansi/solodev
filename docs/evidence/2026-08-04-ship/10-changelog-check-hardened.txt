===== BLOCKER 2 — the three narrow breaks that previously passed =====
All three ran green against the bare-substring version. Each must now fail.

  A) section present but EMPTY, entries left under [Unreleased]
     ERROR CHANGELOG.md's `## [0.2.0]` section is empty. The version was cut but the entries were left behind
     FAIL

  B) '## [0.2.0]' appears only as prose inside [Unreleased]
     ERROR version 0.2.0 has no `## [0.2.0]` section heading in CHANGELOG.md. Bumping without cutting the changelog leaves users no way to see what they received
     FAIL

  C) [Unreleased] renamed with no date and no fresh [Unreleased] opened
     ERROR CHANGELOG.md has no `## [Unreleased]` section. Cutting a release must leave a fresh empty one for the next run to write into
     FAIL

  D) correct cut — dated section WITH entries, fresh [Unreleased] above
     OK

  restored:
     OK
