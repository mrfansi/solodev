===== CLOSING OUT rule-loss AND token-check INLINE =====
Both were spawned by pr-reviewer during run #3. Both went idle twice after
being asked directly for their findings, and never answered. Per the loop's
own rule for an unusable subagent, the phase is redone inline and said so.

--- Q1 (rule-loss): was any rule lost in the run #3 SKILL.md dedup? ---
Its only signal was a summary line: 'Audit done: 1 loss, 0 bad cross-refs'.

Two losses were found and fixed by named agents that DID report:
  1. qa-runner, S1 — 'a feature deferred by the audit cadence becomes the
     next run's first candidate, and the report says so'. In no protocol
     section. Restored.
  2. pr-reviewer  — 'EACH agent's prompt must carry...' vs the protocol's
     'ITS prompt', scoped to qa-runner alone. Restored.

Independent re-sweep of every removed line, against the CURRENT files:
  substantive removed lines checked : 40
  flagged by the crude token-overlap heuristic : 7
  genuinely lost after reading each one by hand : 0

  All seven are line-wrap fragments — half-sentences split across a newline,
  which score low because they are half-sentences, not because the rule is
  gone. Each was read whole and traced: the audit-isolation rationale is §3
  Phase 4/5 plus §11F; "four of the five cannot edit" is a factual claim living
  in README and architecture.md and enforced by the validator; concurrent
  spawning is §3 Phase 4 and 5; fe/be inline is §3 Phase 3 and the `task` half
  was deliberately kept; the layer-1 table is §2. A heuristic that flags
  fragments is exactly the phrase-matching this run's [refactor] learning warns
  about, so the flags were resolved by reading, not by score.
    | Feature or enhancement | Scored normally; prioritised in Phase 1 **unless** this run f
    Audits run as **separate subagents**, never inline. An audit performed by the agent
    that wrote the code grades its own homework, and the protocol forbids approving in
    the pass that authored the change. Four of the five cannot edit files at all, so a
    They are independent and there is nothing to gain by serialising them.
    Implementation tiers — `fe`, `be` — and the backlog filer `task` are **skills, not
    **Layer 1 — state files in the repo** (committed, readable by teammates):

  Cross-references — the '0 bad cross-refs' half of its claim:
    every § pointer in SKILL.md resolves — claim confirmed

  VERDICT: 'a loss' was real and is the one pr-reviewer independently found.
  No third loss survives. Two agents reaching the same finding by different
  routes is the isolation working; the silent one added nothing.

--- Q2 (token-check): do the shipped token figures hold? ---
  It never produced even a summary line.

  Hard token figures surviving in the always-read files (should be 0
  except the fixed run #1 baselines):

  Phase 0 read cost — CORRECTED, and the correction matters

  My first pass here wrote "CHANGELOG fell to 1558 tok". It rose: run #3
  measured ~1066. I stated the flattering half of a two-sided fact and would
  have shipped it. What actually happened:

    whole CHANGELOG.md         ~1550 tok   UP from ~1066
    [Unreleased] section only     ~4 tok   DOWN from ~1175

  Both are true. Protocol §3 Phase 0.3 names `CHANGELOG.md [Unreleased]`, not
  the whole file — so against what Phase 0 actually reads, the release did cut
  that line to nothing, and R-3's CHANGELOG half IS delivered. Against the file
  on disk, cutting a release changes nothing: [0.2.0] lives there forever, so
  the file is still an unbounded growth vector. It just is not one Phase 0 pays
  for.

  A METHODOLOGY ERROR THAT RAN FOR FOUR RUNS: every prior measurement — run #1's
  baseline of ~14240, run #3's ~17900 — counted the WHOLE CHANGELOG file where
  the protocol reads only one section. All of them overstated that line. The
  baselines are not wrong by much, but they were never measuring what they
  claimed to measure.

    specs/LOOP.md                6254 tok
    skills/loop/SKILL.md         2410 tok
    specs/LOOP_STATE.md          6287 tok
    README.md                    1372 tok
    specs/LOOP_LEARNINGS.md      1764 tok
    CHANGELOG.md [Unreleased]       4 tok
    TOTAL                       18091 tok

  Still up from 14240. LOOP_STATE.md is now the largest single read, having
  overtaken the protocol exactly as run #3 predicted it would during run #4.
  R-3 stays top of the backlog and is now unambiguous: LOOP_STATE.md is the
  whole problem.

  VERDICT on token-check: it never reported, and running its brief inline found
  something it might have caught — a four-run measurement-definition error.
