C-4 GITIGNORE CHECK — re-run by QA, independent of run #3's own 02-gate-fails-correctly.txt.
All injected breakage was restored; see the RESTORED markers and the final
`git status --short` at the bottom, which matches the pre-QA working tree exactly.

===== baseline =====

$ python3 scripts/validate.py; echo exit=$?
skills: 11  agents: 5 (4 without edit tools)
OK
exit=0

===== check 1: is the check real, and is --no-index actually required? =====

Injected the exact run #1 bug: appended `specs/` and `docs/` to .gitignore
(both directories are tracked in this repo).

$ python3 scripts/validate.py; echo exit=$?
skills: 11  agents: 5 (4 without edit tools)
ERROR .gitignore excludes specs/, which protocol §2 and §6 require every run to commit. A commit would succeed with it missing, and say nothing
ERROR .gitignore excludes docs/, which protocol §2 and §6 require every run to commit. A commit would succeed with it missing, and say nothing
FAIL
exit=1

Confirms the check fires. Now confirms --no-index is not cargo-culted — plain
`git check-ignore` (what the run's first draft used) is a no-op on this exact bug,
because both paths are already tracked and git skips checking tracked paths:

$ git check-ignore specs; echo "specs exit=$?"
specs exit=1                    (git says: NOT ignored — WRONG, would miss the bug)
$ git check-ignore docs;  echo "docs exit=$?"
docs exit=1                     (same)

$ git check-ignore --no-index specs; echo "specs exit=$?"
specs
specs exit=0                    (correctly detected as ignored)
$ git check-ignore --no-index docs;  echo "docs exit=$?"
docs
docs exit=0                     (correctly detected as ignored)

CONFIRMED: --no-index is load-bearing, not cargo-culted. Without it this check is a
silent no-op on the exact bug it exists to catch, because the tracked-path skip in
plain `git check-ignore` defeats it.

RESTORED: .gitignore copied back from backup, diffed clean, validate.py back to OK.
$ diff /tmp/gitignore.bak .gitignore && echo "RESTORED OK"
RESTORED OK
$ python3 scripts/validate.py; echo exit=$?
skills: 11  agents: 5 (4 without edit tools)
OK
exit=0

===== check 2: agent-count claim scan now covers docs/architecture.md =====

Injected "Four of the five" -> "Three of the five" in docs/architecture.md
(actual count: 4 of 5 agents have no edit tools).

$ python3 scripts/validate.py; echo exit=$?
skills: 11  agents: 5 (4 without edit tools)
ERROR docs/architecture.md: claims 'Three of the five' agents cannot edit, but 4 of the 5 have no edit tools
FAIL
exit=1

RESTORED: docs/architecture.md copied back from backup, .orig cleanup removed,
diff --stat matches the pre-QA diff (12 insertions, 2 deletions vs 33b353e).
$ python3 scripts/validate.py; echo exit=$?
skills: 11  agents: 5 (4 without edit tools)
OK
exit=0

===== NEW: does the check cover the actual highest-risk subpath, not just the two
top-level directories? =====

The [verification] learning this check replaces was general: "before Phase 8 run
`git add -n` on every directory the run created." The C-4 check is narrower: it
only tests whether the literal paths `specs` and `docs` are ignored. Phase 4
evidence lives at `docs/evidence/<date>-<task>/` — a path one level deeper, and the
one a run's own mid-run edits are most likely to touch. Tested whether a gitignore
rule scoped to that subpath, rather than to `docs/` broadly, is caught.

Injected `docs/evidence/` (not `docs/`) into .gitignore:

$ python3 scripts/validate.py; echo exit=$?
skills: 11  agents: 5 (4 without edit tools)
OK
exit=0

validate.py reports OK. `git check-ignore --no-index docs` returns "not ignored"
(the top-level docs/ directory itself is not ignored), so the check never looks at
the subpath at all.

Cross-check with what the RETIRED rule's own mechanism (`git add -n`) would have
caught, using a throwaway file in that exact directory:

$ touch docs/evidence/2026-08-04-skill-dedup/99-injected-test.txt
$ git add -n docs/evidence/2026-08-04-skill-dedup/99-injected-test.txt
The following paths are ignored by one of your .gitignore files:
docs/evidence
hint: Use -f if you really want to add them.
add-n exit=1

CONFIRMED GAP (S1): `git add -n` — the mechanism named in the retired
`[verification]` learning — catches this. `scripts/validate.py`'s replacement check
does not, because it only tests the two literal top-level names `specs` and `docs`,
never a subpath, and never "every directory the run created" (an open-ended set the
old rule covered and the new one does not). `docs/evidence/` is not a hypothetical
path — it is the exact, protocol-mandated (§2) location Phase 4 evidence is written
to every single run, making it the single most plausible target for exactly this
class of bug, and it currently passes the gate silently.

Cleanup: removed the injected test file, restored .gitignore, re-verified.
$ rm docs/evidence/2026-08-04-skill-dedup/99-injected-test.txt
$ diff /tmp/gitignore.bak2 .gitignore   (no output — matches)
$ python3 scripts/validate.py; echo exit=$?
skills: 11  agents: 5 (4 without edit tools)
OK
exit=0

===== final state check =====

$ diff /tmp/gitignore.bak .gitignore && echo "gitignore matches original"
gitignore matches original
$ git status --short
 M CHANGELOG.md
 M docs/architecture.md
 M scripts/validate.py
 M skills/loop/SKILL.md
 M specs/LOOP_LEARNINGS.md
 M specs/LOOP_STATE.md
?? docs/evidence/2026-08-04-skill-dedup/

Matches the working tree exactly as found at the start of this QA pass (plus the
new 04/05/06/07-numbered evidence files this QA session adds, per the evidence
directory being ?? already).
