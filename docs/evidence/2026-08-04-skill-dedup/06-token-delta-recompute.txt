TOKEN-DELTA RECOMPUTE — checking 03-token-delta.txt's headline claim
("~14,240 -> ~17,919 tok, +26%"), which the task flagged as unflattering and
therefore worth checking hard. Estimator kept identical to prior runs: bytes/4,
same six files (specs/LOOP.md, skills/loop/SKILL.md, specs/LOOP_STATE.md,
README.md, specs/LOOP_LEARNINGS.md, CHANGELOG.md).

===== the six-file set itself =====

Verified against specs/LOOP.md §3 Phase 0 step 3: "Read in order:
specs/LOOP_LEARNINGS.md -> specs/LOOP_STATE.md -> CHANGELOG.md [Unreleased] ->
README.md." That is 4 files literally. The other two (specs/LOOP.md,
skills/loop/SKILL.md) are read via skills/loop/SKILL.md's own NORMAL-mode step 1
("Read specs/LOOP.md in full") and via the Skill tool loading SKILL.md itself to
invoke /solodev:loop at all. This six-file convention was established in run #1's
[claims] learning and used unchanged by runs #1-#3. Not disputed here — the set is
right.

===== byte counts: evidence file vs actual current working-tree state =====

$ for f in specs/LOOP.md skills/loop/SKILL.md specs/LOOP_STATE.md README.md specs/LOOP_LEARNINGS.md CHANGELOG.md; do b=$(wc -c < "$f"); echo "$f  bytes=$b"; done

specs/LOOP.md               24603 B   (evidence claimed 24603 B — MATCH)
skills/loop/SKILL.md         8663 B   (evidence claimed  8663 B — MATCH)
specs/LOOP_STATE.md         24976 B   (evidence claimed 24692 B — MISMATCH, +284 B)
README.md                    5374 B   (evidence claimed  5374 B — MATCH)
specs/LOOP_LEARNINGS.md      5145 B   (evidence claimed  5145 B — MATCH)
CHANGELOG.md                 4262 B   (evidence claimed  3197 B — MISMATCH, +1065 B)

CHANGELOG.md's claimed figure (3197 B) is an EXACT match for the file's byte count
at the pre-run-3 baseline commit:

$ git show 33b353e:CHANGELOG.md | wc -c
3197

i.e. 03-token-delta.txt measured CHANGELOG.md BEFORE this run's own edits to it —
before the CHANGELOG entries describing R-2 and C-4 themselves were written. The
[evidence] learning this run's LOOP_STATE.md claims to have "Applied" says exactly
the opposite is required: "a transcript measuring a repo that will contain that
transcript ... regenerate it as the last step before commit." That did not happen
for CHANGELOG.md (and specs/LOOP_STATE.md is also off, likely captured mid-edit
before its own Run Log entry / next-iteration plan were finalised — baseline was
25488 B, current is 24976 B, evidence claims neither).

===== corrected total =====

$ total=0; for f in specs/LOOP.md skills/loop/SKILL.md specs/LOOP_STATE.md README.md specs/LOOP_LEARNINGS.md CHANGELOG.md; do b=$(wc -c < "$f"); total=$((total+b)); done; echo "total bytes=$total tok=$((total/4))"
total bytes=73023 tok=18255

  claimed (03-token-delta.txt) total : ~17,919 tok
  actual current working-tree total  : ~18,256 tok   (73023 / 4 = 18255.75)
  understatement                     : ~336-337 tok

  run #1 baseline                    :  14,240 tok
  claimed delta                      : +3,679 tok  (+25.8%, reported as "+26%")
  actual delta                       : +4,016 tok  (+28.2%)

FINDING (S2): the run's own headline finding is understated by about 2 points of
percentage (+26% claimed vs +28% actual) because two of the six measured files
(CHANGELOG.md most significantly, specs/LOOP_STATE.md to a lesser degree) were
captured at a stale, pre-final-edit snapshot rather than the state that will
actually be committed and read by the next run's Phase 0. This is the precise
failure mode the run's own [evidence] learning exists to prevent, and the run's
LOOP_STATE.md claims that learning was "Applied" to this task — a claim not borne
out by the numbers. The direction of the error is consistent: understating, not
overstating, an already-unflattering number — i.e. the true cost regression is
worse than reported, not better.
