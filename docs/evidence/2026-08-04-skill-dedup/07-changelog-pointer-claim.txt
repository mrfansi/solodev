CHANGELOG / architecture.md "moved to pointers" CLAIM — checked against the actual
skill file, per the task's instruction to check any claim in README, CHANGELOG, or
docs/architecture.md that the refactor made false.

===== the claims =====

CHANGELOG.md [Unreleased] > Changed:
  "skills/loop/SKILL.md no longer restates the protocol. The agent-per-phase table,
  the work-intake and bug-severity table, the branch-and-draft-PR rules, and the
  layer-1 state-file table each existed in both the skill and specs/LOOP.md; the
  skill now points at the section that owns them."

docs/architecture.md:
  "Run #3 finished that job: the agent-per-phase table, the work-intake and severity
  table, the branch-and-draft-PR rules, and the layer-1 state-file table all moved
  to pointers."

Both claims assert all FOUR consolidated blocks became pointers.

===== what the actual diff shows =====

$ git diff 33b353e -- skills/loop/SKILL.md | grep -n "^-##\|^+##\|^-###\|^+###"
-### Work attached to the invocation          (kept as heading, content -> pointer)
-## Companion agents                          (kept as heading, content -> "three things" + pointer)
-### One run, one branch, one PR              (heading DELETED, nothing replaces it)
-## MEMORY (always on, two layers...)          (kept as heading, table -> pointer)

Reading the current skills/loop/SKILL.md directly: after "## Companion agents"
(ending "...this is what to do when that assumption fails.") the very next section
is "## MEMORY (always on, two layers that must not overlap)". There is no
"## One run, one branch, one PR" heading, and no sentence anywhere in the file
pointing a reader at specs/LOOP.md §3 Phase 8 for branch naming, draft-PR
ownership, or the no-remote fallback. Three of the four blocks got an explicit
pointer sentence; this one got none — the section was deleted outright.

$ grep -n "Phase 8\|branch\|draft" skills/loop/SKILL.md
(no output for "branch" or "draft"; "Phase 8" does not appear either)

FINDING (S3): CHANGELOG.md and docs/architecture.md both describe this as "the
skill now points at the section that owns them" / "moved to pointers" for all four
blocks, but one of the four (branch-and-draft-PR) was deleted with zero pointer,
not moved to one. The underlying RULE is not lost — specs/LOOP.md §3 Phase 8 has it
in full, and NORMAL mode step 1 in this same skill file already mandates reading
specs/LOOP.md in full, so a run following the skill will still encounter the rule.
But the specific factual claim about HOW the consolidation was done ("points at the
section", "moved to pointers") is false for this one block. Low real-world risk
(the content is read regardless, via the full-protocol read), so rated cosmetic/
documentation-accuracy rather than functional.
