===== C-4, re-verified after qa-runner S1b =====
The first shipped version checked only the literal roots 'specs' and 'docs'.
qa-runner showed 'docs/evidence/' alone slips through — enough to swallow an
entire run's Phase 4 output while the gate stays green. The check now walks
every subdirectory.

-- baseline --
skills: 11  agents: 5 (4 without edit tools)
OK
   exit=0

-- inject 'specs/' into .gitignore --
   skills: 11  agents: 5 (4 without edit tools)
   ERROR .gitignore excludes specs/, which protocol §2 and §6 require every run to commit. A commit would succeed with it missing, and say nothing
   FAIL
   exit=

-- inject 'docs/' into .gitignore --
   skills: 11  agents: 5 (4 without edit tools)
   ERROR .gitignore excludes docs/, which protocol §2 and §6 require every run to commit. A commit would succeed with it missing, and say nothing
   ERROR .gitignore excludes docs/evidence/, which protocol §2 and §6 require every run to commit. A commit would succeed with it missing, and say nothing
   ERROR .gitignore excludes docs/flows/, which protocol §2 and §6 require every run to commit. A commit would succeed with it missing, and say nothing
   ERROR .gitignore excludes docs/evidence/2026-08-04-discover/, which protocol §2 and §6 require every run to commit. A commit would succeed with it missing, and say nothing
   ERROR .gitignore excludes docs/evidence/2026-08-04-skill-dedup/, which protocol §2 and §6 require every run to commit. A commit would succeed with it missing, and say nothing
   ERROR .gitignore excludes docs/evidence/2026-08-04-bootstrap/, which protocol §2 and §6 require every run to commit. A commit would succeed with it missing, and say nothing
   FAIL
   exit=

-- inject 'docs/evidence/' into .gitignore --
   skills: 11  agents: 5 (4 without edit tools)
   ERROR .gitignore excludes docs/evidence/, which protocol §2 and §6 require every run to commit. A commit would succeed with it missing, and say nothing
   ERROR .gitignore excludes docs/evidence/2026-08-04-discover/, which protocol §2 and §6 require every run to commit. A commit would succeed with it missing, and say nothing
   ERROR .gitignore excludes docs/evidence/2026-08-04-skill-dedup/, which protocol §2 and §6 require every run to commit. A commit would succeed with it missing, and say nothing
   ERROR .gitignore excludes docs/evidence/2026-08-04-bootstrap/, which protocol §2 and §6 require every run to commit. A commit would succeed with it missing, and say nothing
   FAIL
   exit=

-- all restored --
skills: 11  agents: 5 (4 without edit tools)
OK
   exit=0

-- directories the check now covers --
   specs
   docs
   docs/evidence
   docs/flows
   docs/evidence/2026-08-04-discover
   docs/evidence/2026-08-04-skill-dedup
   docs/evidence/2026-08-04-bootstrap
