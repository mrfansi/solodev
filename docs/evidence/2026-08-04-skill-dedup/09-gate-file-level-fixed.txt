===== C-4, third rebuild — after pr-reviewer blocker 1 =====
v1  plain check-ignore          : skips tracked paths, could never fire
v2  --no-index, roots only      : missed docs/evidence/ (qa-runner S1)
v3  --no-index, roots + subdirs : missed *.txt (pr-reviewer blocker 1)
v4  files AND dirs, via --stdin : below

Each version passed the obvious injection and failed a narrower one. That
is now a recorded learning; this transcript is the third instance of it.

-- baseline, real repo --
   OK
   inject 'specs/'           -> FAIL (5 errors)
   inject 'docs/'            -> FAIL (31 errors)
   inject 'docs/evidence/'   -> FAIL (26 errors)
   inject '*.txt'            -> FAIL (22 errors)
   inject 'specs/*.md'       -> FAIL (4 errors)
   inject 'docs/evidence/**' -> FAIL (25 errors)

-- git add -n cross-check: does the gate agree with the old oracle? --
   with '*.txt':
     gate  : FAIL
     add -n: 0 paths stageable (0 = all swallowed)

-- outside a git repo, the check must NOT read as a pass --
   First attempt at this test was invalid: it ran validate.py in a bare temp
   directory with no manifests, so it crashed on plugin.json long before
   reaching the check. Redone against a full non-git copy of the repo:
     git rev-parse --is-inside-work-tree -> fatal: not a git repository
     git check-ignore --no-index --stdin -> exit 128
     validate.py                         -> ERROR "git check-ignore could not
                                            run (...); the deliverable-not-
                                            ignored check did NOT execute"
   The gate refuses to report a pass for a check that never ran. Before this
   fix, returncode 128 produced empty stdout and read as clean.

-- all restored --
   OK
